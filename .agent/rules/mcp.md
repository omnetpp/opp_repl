# opp_repl MCP Rules

- Always use the opp_repl MCP server for simulation tasks instead of shell commands.
- Do NOT search in files, do NOT read files, do NOT run shell commands unless you checked that there is no explicit opp_repl support for the given functionality.
